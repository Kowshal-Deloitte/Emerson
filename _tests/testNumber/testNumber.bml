importtransactiondata(16274150);

expected = "Quote #48";

actual = commerce.quoteNumber();
 
if (actual <> expected) {
  print actual + " when " + expected + " was expected";
  return false;
}
 
print "Success! No asserts failed!";
return true;